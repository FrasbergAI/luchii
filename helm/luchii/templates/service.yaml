apiVersion: v1
kind: Service
metadata:
  name: luchii
spec:
  type: {{ .Values.service.type }}
  selector:
    app: luchii
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}