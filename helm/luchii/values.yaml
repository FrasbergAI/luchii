image:
  repository: ghcr.io/frasbergai/luchii-engine
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 9000

resources:
  limits:
    cpu: "1"
    memory: 2Gi
  requests:
    cpu: "0.5"
    memory: 1Gi

env:
  REGION: "us-west"